

def autenticate_user(_p_url: str, _p_user_email: str, _p_user_password: str):
    """
    Autenticate with user in application

    parameters:

    - _p_url: str
    - _p_user_email: str
    - _p_user_password: str

    return autentication token access
    """
    return None
